site_name: evoc-rs
site_description: EVoC clustering for high-dimensional embeddings, in Rust.
site_url: https://gregorlueg.github.io/evoc-rs/
repo_url: https://github.com/GregorLueg/evoc-rs
repo_name: GregorLueg/evoc-rs
edit_uri: edit/main/python/docs/
copyright: MIT and BSD-2-Clause licensed

theme:
  name: material
  features:
    - navigation.sections
    - navigation.top
    - navigation.tracking
    - content.code.copy
    - content.action.edit
    - toc.follow
  palette:
    - media: "(prefers-color-scheme: light)"
      scheme: default
      primary: indigo
      accent: indigo
      toggle:
        icon: material/weather-night
        name: Dark mode
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      primary: indigo
      accent: indigo
      toggle:
        icon: material/weather-sunny
        name: Light mode

nav:
  - Home: index.md
  - Quickstart: quickstart.md
  - The cluster hierarchy: hierarchy.md
  - kNN backends: backends.md
  - GPU: gpu.md
  - API reference:
      - Top level: api/toplevel.md
      - Estimators: api/estimator.md

markdown_extensions:
  - admonition
  - attr_list
  - md_in_html
  - tables
  - toc:
      permalink: true
  - pymdownx.details
  - pymdownx.highlight:
      anchor_linenums: true
  - pymdownx.inlinehilite
  - pymdownx.superfences

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          # `evoc_rs` sits next to mkdocs.yml, not under `docs/`.
          paths: ["."]
          options:
            docstring_style: google
            # The `Args:` sections carry one row per parameter, which is the
            # whole point of writing them out; a table is how that reads.
            docstring_section_style: table
            # Constructor signature and class docstring render as one block,
            # so a reader sees the defaults next to what they mean.
            merge_init_into_class: true
            separate_signature: true
            show_signature_annotations: true
            signature_crossrefs: true
            members_order: source
            show_root_heading: true
            show_root_full_path: false
            show_source: false
            show_symbol_type_heading: true
            show_symbol_type_toc: true
            heading_level: 2
            filters: ["!^_"]

# `mkdocs serve` picks up a docstring edit without a restart.
watch:
  - evoc_rs
